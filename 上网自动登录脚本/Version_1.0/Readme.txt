
1.Netlaunch.py 是用python3编写的脚本
2.test.bat是Windows 的批处理脚本，用来开机自启动.py脚本。

注意
计算机对在cmd中输入的python xx.py 这条命令是需要有默认python编译器的。（也就是我们说的环境变量）
由于我使用了虚拟环境用来区分不同的工程，也就是每个虚拟环境的第三方的包不一样。
Netlaunch.py 的文件是基于anaconda的虚拟环境learn开启的
所以，计算机的环境变量python 的路径是D：\Anaconda\envs\learn

